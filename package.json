{
  "name": "organ-tunggal",
  "version": "1.0.0",
  "description": "Organ Tunggal Player App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed'",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
